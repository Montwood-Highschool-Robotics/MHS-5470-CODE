package org.firstinspires.ftc.teamcode.Subsystems.drivetrain;

import com.pedropathing.geometry.Pose;

/**
 * All target points used by Pedro. Defaults are RED.
 */
public class TargetPoints {
    public static final Pose CLOSE_START = new Pose(125, 125, Math.toRadians(135));
    public static final Pose FAR_START = new Pose(80, 8, Math.toRadians(90));
    public static final Pose CLOSE_SHOOT = new Pose(72, 72, Math.toRadians(135));
    public static final Pose FAR_SHOOT = new Pose(0,0,0);
}
